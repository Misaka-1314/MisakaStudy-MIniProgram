# AGENTS.md

本文件为在本仓库工作的编程智能体（AI agent）必须遵守的协作与安全规范。

## 提交与合并流程

- **禁止直接 push 代码到主干**。所有改动必须按功能拆分提交 commit（每个 commit 只包含一个逻辑内聚的功能或修复），然后使用 `gh` 在 GitHub 上创建 Pull Request（PR），审查通过后合并，最后删除多余的分支。
- 标准流程：
  1. 从 `main` 新建功能分支（如 `feature/xxx`、`fix/xxx`）；
  2. 在分支上按功能粒度逐个提交 commit；
  3. 使用 `gh pr create` 创建 PR；
  4. PR 通过审查后合并；
  5. 合并后删除该功能分支（本地与远程）。

## 安全提醒（开源仓库）

- 本仓库是**开源仓库**，任何提交内容都会公开展示。
- 严禁提交或推送任何隐私/敏感信息，包括但不限于：**密码、密钥、Token、内网 IP 地址、手机号、个人身份信息**等。
- 每次提交前务必检查 diff，确认没有泄漏敏感信息后再提交。

## 提交签名

- 本仓库**所有提交都必须签名**。
- 签名方式为 SSH 私钥签名，相关签名配置已写入**全局 git 配置**，请勿在本文件中记录密钥路径等敏感配置。
- 提交者信息（`user.name` / `user.email`）同样配置在全局 git 配置中，请勿在此重复。
